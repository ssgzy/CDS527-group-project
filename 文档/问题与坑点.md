# 问题与坑点

## 当前已发现问题

### 1. 截止日期存在明显矛盾
- 文档标题：`CDS527 Big Data Analytics 2025-2026 Term2`
- 提交说明：`30-April-2025 23:59 HK time`
- 判断：这个日期很可能是旧模板残留或笔误，必须以 Moodle 或老师最新通知为准

### 2. 提交数量描述不一致
- 文档文字写的是 `submit three files`
- 但实际列出的组内提交物有 4 项：
  - Notebook
  - Report
  - PPT
  - Separate GAI declaration sheet
- 另外还要求每位学生再单独上传一份 `work distribution` 文件

### 3. data split / metric 未由老师指定（已自行制定）
- 本地目录中已存在全部需要的输入文件
- 老师在现有文档中未明确指定 standardized data split 与 evaluation metric
- **已处理**：团队自行制定（stratified 80/20，seed=42，主指标 macro-F1），并在 [[实验协议]] 和 Notebook 中说明选择理由

### 4. 数据说明与实际 CSV 规模不一致
- `SMILE Twitter Emotion Dataset` 说明文档写的是 `3,085 tweets`
- 本地 `smile-annotations-final.csv` 实际只有 `1299` 行数据
- 当前判断：
  - 可能是子集版本
  - 可能有清洗或过滤
  - 也可能当前 CSV 不是老师最终指定版本

### 5. PySpark 是硬性要求
- 文档明确说 PySpark 必须是主要开发工具
- 过度依赖标准 Python 会扣分
- 后续做实验时必须持续检查实现方式，避免 notebook 最终呈现不符合要求

### 6. 当前数据类别极不平衡
- `happy` 类远高于其他类别
- 如果只看 accuracy，结果可能具有误导性
- 后续必须结合老师指定指标来设计实验汇报

### 7. PySpark 3.1.2 与 Java 版本冲突（2026-03-31 发现）
- 系统默认 JAVA_HOME 指向 conda 内 Java 17（Zulu）
- PySpark 3.1.2 与 Java 17 存在 module access 限制（`InaccessibleObjectException`），SparkSession 无法启动
- **解决方案**：启动 PySpark 前必须指定 Java 11：
  `export JAVA_HOME=/Library/Java/JavaVirtualMachines/amazon-corretto-11.jdk/Contents/Home`
- 已安装：Amazon Corretto 11（/Library/Java/JavaVirtualMachines/）

### 8. PySpark 读取 CSV 行数错误（2026-03-31 发现）
- 默认 `spark.read.csv(header=True, inferSchema=True)` 读取后得到 1355 行（含 97 个 null emotions）
- 原因：部分 tweet 正文含换行符，默认读取将其拆成多行
- **解决方案**：必须加 `multiLine=True, quote='"', escape='"'`，读取后行数正确为 1299 行，零 null

### 9. 过程文档与最终交付物不同步（2026-04-08 审计）
- 根目录已存在正式 PPT：`CDS527.pptx` 与 `Text_Classification_and_Google_Strategic_Audit.pptx`
- 但多个过程文档仍写成“正式 PPT 尚未创建”
- 这说明 2026-04-04 之后的部分执行结果没有完整回写到 Obsidian

### 10. AI 工具记录未统一（2026-04-08 审计）
- `工作区/Group_X.gai.docx` 当前只写了 `ChatGPT (OpenAI GPT-4)`
- 但项目后期已使用 `Claude Code`
- 如果最终提交文件确实受 Claude Code 辅助，GAI 声明与过程文档必须同步修正，避免申报不完整

## 状态（2026-03-31 更新）

| 问题 | 状态 |
|------|------|
| 截止日期年份矛盾（写 2025，应为 2026） | ⚠️ 未确认，建议向老师核实 |
| 提交数量描述不一致（"三个文件"但列出五项） | ⚠️ 未确认，建议向老师核实 |
| data split / metric 未由老师指定 | ✅ 已自行制定，见 [[实验协议]] |
| 数据说明与 CSV 规模不一致（3085 vs 1299） | ℹ️ 在 Notebook 和报告中说明为"子集版本" |
| PySpark 3.1.2 与 Java 17 冲突 | ✅ 已修复，使用 Java 11 Amazon Corretto |
| PySpark CSV multiLine bug | ✅ 已修复，统一使用 multiLine=True |
| 正式 PPT 已生成但过程文档未同步 | ⚠️ 需补齐状态和收尾记录 |
| GAI 声明未反映 Claude Code | ⚠️ 需修正工具申报内容 |

## 相关链接
- [[项目总览]]
- [[当前状态]]
- [[任务笔记 - 阅读作业要求]]
- [[任务笔记 - Task1 内容梳理]]
- [[数据说明]]
- [[下一步]]
