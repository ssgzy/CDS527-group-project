# 当前状态

## 状态摘要
- 更新时间：2026-04-13 11:25:05 HKT
- 当前阶段：Task 1 缺失实验已补做完成，输出目录已按 section 重整，Notebook 已追加补充章节
- 下一步：把新的最优配置与新增结果统一同步到 PPT、report、GAI 声明与最终提交口径

## 已完成（Step 1–9）

- Step 1–5：现状检查、文档补齐、环境验证、数据审查、实验协议固化 ✅
- Step 6：Baseline（TF-IDF + LR，macro-F1=0.2337）✅
- Step 7：Section 2 模型比较（最优：CNB=0.3332）✅
- Step 8：Section 3 表示方法比较（在固定 LR 的当前设置下，BOW 系列无差异；Word2Vec 更差）✅
- **Step 9：Section 4 改进 & 深度分析 ✅**
  - LR + 逆频率类别权重，regParam ∈ {0.01, 0.05, 0.1, 0.5, 1.0}
  - 原主线最优：LR+Weight (rp=0.5)，macro-F1=0.3453
  - 深度分析：混淆矩阵、per-class metrics、最难混淆对、训练时间 vs 性能
- **Step 10：补做实验与输出整理 ✅**
  - Section 3 Part B：GloVe、BERT embedding
  - Section 4：MLP + Word2Vec、ablation study
  - `输出/` 已新增 `sections/` 与 `tables/`
  - Notebook `工作区/Group_X.code.ipynb` 已在最底部追加 `Section 10 — Supplementary Experiments (Appended)`，未插入原主线代码块
- Task 2 case study 文档 `工作区/Group_X.report.docx` ✅
- GAI 声明 `工作区/Group_X.gai.docx` ✅
- work distribution 模板 `工作区/work_distribution.docx` ✅
- 汇报大纲 `工作区/Group_X_PPT_outline.md` ✅
- 正式 PPT 成品 `CDS527.pptx` / `Text_Classification_and_Google_Strategic_Audit.pptx` ✅（两者内容相同）

## 结果总览

| Section | 最佳配置 | macro-F1 |
|---------|---------|---------|
| S1 Baseline | TF-IDF (uni) + LR | 0.2337 |
| S2 Model Comparison | TF-IDF (uni) + CNB | 0.3332 |
| S3 Repr Comparison | BERT embedding + LR | 0.3420 |
| S4 Improvement（原主线） | LR+Weight (rp=0.5) | 0.3453 |
| **S4 Ablation（当前全项目最优）** | **A4 Light cleaning only + TF-IDF + class weight + LR** | **0.4465** |

## 进行中
- 无代码实验进行中

## 待处理
- `工作区/Group_X.code.ipynb` 已追加 Section 10，但当前保存状态中 `execution_count` 与 `outputs` 仍为空，需要决定是否以执行后状态提交
- `Group_X_PPT_outline.md` 中部分 Task 1 数值与 `输出/data/` 中正式结果不一致；需确认是否已经在最终 PPT 中修正
- 正式 PPT 当前有两个同内容文件，命名不统一，需确定唯一提交版本
- `工作区/Group_X.gai.docx` 当前只写了 `ChatGPT (OpenAI GPT-4)`，未反映后续使用的 Claude Code
- 现在不能再沿用旧口径“当前最优是 0.3453”；需统一更新为 `A4 = 0.4465`
- 需要确认是否把 `Section 10` 的补做实验写进最终 report / PPT 主叙事，还是保留为 appendix / supplementary

## 文档产出状态
- `文档/` 目录：完整
- `输出/figures/`：12 张图（fig1–fig12）
- `输出/reports/`：报告齐全（EDA + S1–S4 + S3 Part B + ablation）
- `输出/data/`：6 个结果 CSV（S2、S3A、S3B、S4 主线、S4 MLP、S4 ablation）
- `输出/tables/`：4 组总表 / 排名表 / per-class F1 / 路线图状态表
- `输出/sections/`：已按 Section 重新整理
- `输出/README.md`：已更新，覆盖新增目录结构与最新最佳结果
- `文档/结论层级汇总.md`：S1/S2/S3/S4 四层结论全部完整
- `文档/Overall Summary 素材.md`：第 1–6 节全部填写完毕

## 关键技术备注
- JAVA_HOME 必须指向 Java 11（Notebook Section 0 已包含）
- CSV 读取必须 `multiLine=True`
- 缓存管理：每个模型预测后 `.unpersist()`，train/test 最后释放
- **Notebook**：`工作区/Group_X.code.ipynb`（Section 0–9 原主线保留；Section 10 以补充形式追加；当前文件未保存执行输出）

## 相关链接
- [[项目总览]]
- [[实验协议]]
- [[结果索引]]
- [[结论层级汇总]]
- [[Overall Summary 素材]]
- [[任务笔记 - Task1 内容梳理]]
- [[下一步]]
- [[会话日志]]
